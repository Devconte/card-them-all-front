# Configuration pour VPS - accessible via IP
server {
    listen 80;
    server_name vps-71464054.vps.ovh.net _;
    
    # Proxy vers le frontend
    location / {
        proxy_pass http://frontend:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Gestion du routing côté client
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Cache des assets statiques
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# Configuration SSL (à décommenter une fois le nom de domaine configuré)
# server {
#     listen 80;
#     server_name cardthemall.fr www.cardthemall.fr;
#     
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
# 
# server {
#     listen 443 ssl;
#     server_name cardthemall.fr www.cardthemall.fr;
#     
#     ssl_certificate /etc/letsencrypt/live/cardthemall.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cardthemall.fr/privkey.pem;
#     
#     # Configuration SSL moderne
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     location / {
#         proxy_pass http://frontend:5173;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#     
#     # Gestion du routing côté client
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#     
#     # Cache des assets statiques
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# } 