services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "3001:3001"
    networks:
      - app-network   
networks:
  app-network:
    name: card-them-all-front_app-network
    driver: bridge 